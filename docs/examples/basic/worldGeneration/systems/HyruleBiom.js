import Bush from "../entities/Bush"

export default class HyruleBiom {
    constructor(){
        this.pool = {
            bush: []
        };
    }

    createBush({x, y}){
        if (this.pool.bush.length >0){
            this.pool.push
        }
    }

    afterUpdate(){}
    beforeUpdate(){}
}