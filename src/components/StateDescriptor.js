export default class StateDescriptor {
    constructor() {
        this.name = null; // This is tied with the name of the state.
        this.label = null; // This is what the users see.
        this.description = null;
        this.optionProperties = [];
    }
}