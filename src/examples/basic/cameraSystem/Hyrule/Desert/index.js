import Range from "../../Range";

export default class Hyrule {
    constructor(){
        this.name = "hyrule";
        this.range = new Range(0.45, 1);
    }

    createGroundEntities({terrainHeight, noise}){

    }

    releaseEntities(entities){

    }
}