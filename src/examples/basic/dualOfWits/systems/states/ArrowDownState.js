export default class ArrowDownState {
    constructor() { }

    activated(entity) { 
        let imageTexture = entity.getComponent("image-texture");
        

    }

    deactivated(entity) { }

    initialized() { }

    update(entity) { }
}